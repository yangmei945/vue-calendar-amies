import vueCalendarAmies from './vue-calendar-amies.vue'

const calendaramies = {
    install(Vue, options){
        Vue.component(vueCalendarAmies.name, vueCalendarAmies)
    }
}
export default calendaramies
